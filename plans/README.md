# Implementation Plans

Generated by the improve skill on 2026-07-26, against commit `0346777` on `main`.

Execute in the order below unless the dependency notes say otherwise. Each
executor: **read the plan fully before starting**, run its drift check first,
honor its STOP conditions, and update your row in the table when done.

Every plan is self-contained. You do not need to have read the audit, any other
plan, or any prior conversation — all context, code excerpts, conventions and
commands are inlined in the plan file itself.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Stop the Settings page claiming a notification that never fires | P1 | S | — | TODO |
| 002 | Stop the SMART tab wedging forever on a dead collector's progress file | P1 | S | — | TODO |
| 003 | Pin and checksum-verify the binaries build.sh downloads | P1 | S | — | TODO |
| 004 | Read Performance-tab temperatures per controller instead of by position | P1 | S | — | TODO |
| 005 | Claim the flash single-flight lock atomically | P1 | S | — | TODO |
| 006 | Make the AJAX render layer testable, and test it | P2 | M | — | TODO |
| 007 | Escape every hardware-sourced value in the AJAX renderers | P2 | S | 006 | TODO |
| 008 | Parse lsblk output by key, not by column position | P3 | S | — | TODO |
| 009 | Verify Unraid's CSRF token server-side instead of assuming the platform did | P2 | S | 005 (sequencing) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Branching

Work happens on `dev`, not `main`.

- `dev` is the integration branch and is currently level with `main` at `0346777`.
- Each plan says `Branch: advisor/NNN-<slug>`. **Cut that branch from `dev`**
  (`git switch dev && git switch -c advisor/NNN-<slug>`) and merge it back to
  `dev` when its done criteria pass.
- Nothing reaches `main` until it has been tested on real hardware — see the
  table below for which plans that actually applies to.
- No plan pushes or opens a PR on its own; that stays a human decision.

## Testing on a live Unraid box

The plugin is plain PHP and shell with no build step, so there is no need to cut
a release to test a change. Copy the tree straight onto the server, over the
installed copy:

```bash
# from the repo root, on the branch you want to test
rsync -rlpv --delete \
  --exclude 'chart.umd.min.js' --exclude 'hbaviewer.x86_64' \
  source/usr/local/emhttp/plugins/hbaviewer/ \
  root@tower:/usr/local/emhttp/plugins/hbaviewer/
```

Three details that will otherwise waste your afternoon:

1. **The two excludes are mandatory.** `chart.umd.min.js` and `hbaviewer.x86_64`
   are downloaded by `build.sh`, are git-ignored, and are not in the repo. With
   `--delete` and no excludes, rsync removes them from the server and the
   Performance tab and the whole SAS2 path break until you reinstall.
2. **`-rlpv`, not `-a`.** `-a` implies `-t`, which preserves your local mtimes.
   `get_hba_info.sh` invalidates its cache by comparing the cache's mtime against
   *its own* (`get_hba_info.sh:22`), so a preserved older mtime means your new
   code sits behind a stale cache and appears to do nothing. Without `-t`, rsync
   stamps the files as new.
3. **Clear the caches after syncing**, or wait 60 seconds and wonder why nothing
   changed:

   ```bash
   ssh root@tower 'rm -f /tmp/lsiutil_dash.json /tmp/hbav_overview.out \
     /tmp/lsiutil_smart.json /tmp/lsiutil_smart.json.progress'
   ```

To go back to the released version: reinstall the plugin from **Plugins →
Install Plugin** using the URL in the README. Nothing here writes to
`/boot/config/plugins/hbaviewer/`, so your settings and event archive survive.

### Which plans actually need the box

| Plan | Hardware test | What to check |
| ---- | ------------- | ------------- |
| 001 | Not needed | Optional: the Alert Threshold help text on the Settings page reads correctly |
| 002 | **Yes — this is the proof** | Plan's Test plan has the exact wedge repro: plant a stale `.progress` file, confirm the tab recovers instead of hanging forever |
| 003 | Not needed | Needs network, not hardware. Runs entirely on your workstation |
| 004 | **Yes** | A SAS2 box shows Defect A most clearly (temperature graph currently flat/empty on the Performance tab). Multi-controller shows the misattribution |
| 005 | Partly — **do not test by flashing** | Verify the refusal paths only: submit with the array started, confirm it is refused and no `flash.lock` is left behind. Never exercise this against a card you care about |
| 006 | Not needed | Pure refactor plus tests. Cheap smoke test: open each tab and confirm the tables render identically to before |
| 007 | Not needed | Same smoke test as 006; the tables should be visually unchanged |
| 008 | **Yes, if you have a drive with no serial** | Run `lsblk -S -P -o NAME,WWN,SERIAL,MODEL` on the box first. If no row shows `SERIAL=""` you cannot reproduce the original bug, only confirm the rewrite still works |
| 009 | **Yes — answers the open question** | Its Step 8. Note whether settings saving worked *before* the change on your Unraid version — that is the direct answer to whether the platform enforces CSRF |

Plans 005, 006, 007 and 009 add or depend on PHP unit tests. Your Unraid box has
`php`, so `bash tests/run.sh` runs the full suite there even though it cannot on
a workstation without `php` or Docker.

## Dependency notes

- **007 requires 006.** 007 edits rendering logic; 006 builds the test file that
  proves those edits changed nothing else. Running 007 first means changing the
  most-viewed code in the plugin with no regression net at all.
- **009 should follow 005.** Both modify `flash.php`. 005 is higher priority and
  its diff is smaller, so landing it first avoids a merge conflict in the one
  file where a bad merge can brick hardware. This is sequencing only — neither
  depends on the other's behaviour.
- **001–005 and 008 are fully independent** of each other and of everything
  else. They can be done in any order, or in parallel by different executors.

## Where the risk is

Three plans deserve more review attention than the others:

- **005** touches the code path that flashes firmware. The change is four lines
  and the plan spells out the one condition (`if ($owned)`) that must not be
  dropped, but a mistake here is a bricked controller rather than a broken page.
- **006** is a refactor of the largest untested file in the repo. It must be
  provably inert — the plan builds in diff-based inertness checks at three
  points precisely because "the tests pass" proves nothing when the tests were
  written after the refactor.
- **009** is fail-closed on the settings page. If the assumption behind it is
  wrong on a given Unraid version, the symptom is a user unable to save settings.
  Its Step 8 hardware check exists to catch that, and its STOP conditions
  explicitly forbid "fixing" it by weakening the check.

## Verification baseline (as of `0346777`)

Confirmed by running it, so executors can tell their failures from pre-existing ones:

- `bash tests/run.sh` — shell half **passes** (parser goldens + all 14 flash
  tests) under GNU Awk 5.3.2.
- The PHP half needs a local `php` or a working Docker daemon; `tests/run_php.sh`
  falls back to `php:8.2-cli` automatically. It was **not** runnable in the
  advisor's environment, so it is CI-verified only. Plans 005, 006, 007 and 009
  add or depend on PHP tests and require one of the two to be available.
- CI (`.github/workflows/php.yml`) lints every `.php` with `php -l` and every
  `.sh` under `source`/`tests` with `bash -n`, then runs the full suite.
  `build.sh` at the repo root is **not** linted by CI — plan 003 notes this.

## Findings considered and rejected

Recorded so they are not re-audited on the next pass:

- **Command injection in the `shell_exec` calls** — every one interpolates either
  a fixed constant or an `escapeshellarg`-wrapped value. Checked all call sites
  in `ajax_info.php`, `flash.php`, `dashboard.php`, `settings.php` and
  `cached_read.php`. Nothing found.
- **Flash guardrails insufficient** — they are not. The opt-in toggle,
  array-stopped check, typed `FLASH` confirmation, scoped read-only verify,
  `flash_safe_name` upload confinement and the `FLASH_DIR` prefix check all hold
  server-side and are unit-tested. Only the lock acquisition was wrong (plan 005).
- **Config validation** — `config.php` types and range-clamps every value on both
  read and write, and is covered by `tests/config_test.php`. Solid as-is.
- **Predictable `/tmp` paths** (`hbav_overview.out`, `lsiutil_smart.json`,
  `hbav_flash/`) enabling symlink attacks — not worth code on a single-root
  appliance where anyone who can create those files is already root.
- **gawk-only three-argument `match()`** in `parse/storcli_drives.sh` — deliberate.
  CI installs gawk and makes it the default `awk`; Unraid's Slackware base ships
  it. Not a portability bug.
- **Bricking risk from a mismatched firmware image** — documented, by design, and
  user-supplied. The README is explicit about it and the UI requires a typed
  confirmation. Not a defect.
- **Real temperature alerting** (cron + Unraid `notify`) — a *feature*, not a
  bug, and deliberately excluded from plan 001, which only removes the false
  claim that it already exists. Tracked below as direction work.

## Direction (not planned — options for the maintainer)

Surfaced by the audit but not turned into plans, because these are product
decisions rather than defects:

1. **Real alerting.** The plugin already computes a health rollup covering failed
   drives, PHY error counters and pre-P20 firmware, and can currently tell nobody
   about any of it. A cron script calling
   `/usr/local/emhttp/webGui/scripts/notify` on the existing `status` field would
   turn a dashboard into a monitor. Needs last-notified state to avoid spamming.
2. **Persist performance history.** The Performance tab is in-browser only and
   resets on reload — a deliberate no-daemon, no-flash-writes decision. But "was
   the controller saturated at 3am?" is what people open it to ask. A bounded ring
   buffer in `/tmp` would answer it without a sampler daemon.
3. **Trend PHY error *rate*, not the counter.** Counters are cumulative since
   boot, so a card that logged 40k invalid DWords two months ago looks identical
   to one degrading now. Periodic snapshots plus a delta would make the PHY tab
   diagnostic instead of descriptive — the highest-signal failure predictor
   already being collected.

## Scope not audited

For the next pass: `parse/{phy,events,smart,drives_join,storcli_phy,storcli_events,storcli_enclosures}.sh`,
the PHP test files themselves, `scripts/capture*.sh`, the install/remove blocks
in `hbaviewer.plg`, and `dashboard.php` past line 60.
